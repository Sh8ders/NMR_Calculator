[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nmr-calculator"
version = "0.3.0"
description = "Phase 3 scaffold for future 1H NMR proton environment grouping and prediction."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer",
    "numpy",
    "pandas",
    "matplotlib",
    "rdkit",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[project.scripts]
nmr-calculator = "nmr_calculator.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
