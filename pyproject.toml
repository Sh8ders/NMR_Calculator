[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nmr-calculator"
version = "0.10.0"
description = "Phase 10 scaffold with a clean 1H NMR predictor interface."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer",
    "numpy",
    "pandas",
    "matplotlib",
    "rdkit",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[project.scripts]
nmr-calculator = "nmr_calculator.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
