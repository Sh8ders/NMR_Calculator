[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nmr-calculator"
version = "0.11.0"
description = "Phase 11 scaffold with local 1H NMR shift database loading."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer",
    "numpy",
    "pandas",
    "matplotlib",
    "rdkit",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[project.scripts]
nmr-calculator = "nmr_calculator.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
